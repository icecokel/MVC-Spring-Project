<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="usermapper">
	<select id="login" parameterType= "java.lang.String" resultType= "IceUser">
		select * from users 
		where email =#{email}
	</select>
	
	<select id="checkemail" resultType = "java.lang.String" parameterType ="java.lang.String" >
		select email
		from users
		where email = #{email}
	</select>
	
	<select id="checknickname" resultType = "java.lang.String" parameterType ="java.lang.String">
		select nickname
		from nickname
		where nickname = #{nickname}
	 </select>
	
	
	<insert id="join" parameterType="IceUser">
		insert into users (email, password, name ,  nickname , image, birthday)
		values(#{email}, #{password}, #{name},#{nickname} ,#{image} ,#{birthday} )
	</insert>
	
</mapper>
